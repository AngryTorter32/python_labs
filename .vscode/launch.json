{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run text_report",
            "type": "python",
            "request": "launch",
            "module": "src.lab04.text_report",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}